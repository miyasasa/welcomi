stages:
  - release

release:
  stage: release
  image:
    name: goreleaser/goreleaser
    entrypoint: [ '' ]
  only:
    - master
  variables:
    GIT_DEPTH: 0
  script:
    - goreleaser release --rm-dist